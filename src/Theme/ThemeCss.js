function ThemeCss(props) {
  /* const { theme } = props;

  const body = document.querySelector("body");
  const main = document.querySelector("main");
  const icons = document.getElementsByClassName("icon");
  console.log(main)
  let iconBrightness;
  if (theme === "dark") {
    body.style.backgroundColor = "#101010";
    main.style.backgroundColor = "#171717";
    iconBrightness = "1";
  } else if (theme === "light") {
    body.style.backgroundColor = "#DDDDDD";
    main.style.backgroundColor = "#404040";
    iconBrightness = ".25";
  }
  if (theme) {
    for (let icon of icons) {
      icon.style.filter = `brightness(${iconBrightness})`;
    }
  } */
}

export default ThemeCss;
